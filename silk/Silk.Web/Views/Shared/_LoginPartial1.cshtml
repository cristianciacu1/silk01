@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


        <li class="nav-item dropdown">

            @* Desktop *@
            <a class="nav-link dropdown-toggle text-dark noHover d-none d-sm-block" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <img src="~/Icons/account.png" alt="Cart" style="width: 22px; height: 22px" />
            </a>

            @* Mobil *@
            <a class="nav-link noHover d-block d-sm-none" asp-area="Identity" asp-page="/Account/Manage/Index">
                <img src="~/Icons/account.png" alt="Cart" style="width: 22px; height: 22px" />
            </a>

            @if (SignInManager.IsSignedIn(User))
            {

                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <div class="container">
                        <h5>Bine ai venit, </h5>
                        <h5 class="text-center">@User.Identity.Name!</h5>
                        <form class="form-inline" asp-area="Identity" asp-page="/Account/Manage/Index">
                            <button type="submit" class="btn btn-dark butonCuMargini" style="width: 100%;">
                                Contul tau
                            </button>
                        </form>
                        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                            <button type="submit" class="btn btn-dark" style="width: 100%;">Logout</button>
                        </form>
                    </div>
                </div>
            }
            else
            {
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <div class="container">
                        <button type="button" class="btn btn-dark butonCuMargini" style="width: 100%;">
                            <a class="text-light noHover" asp-area="Identity" asp-page="/Account/Login">Login</a>
                        </button>
                    </div>
                </div>
            }
        </li>
